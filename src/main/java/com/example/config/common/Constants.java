package com.example.config.common;

/**
 * this is intended to keep the Constants in one place
 * easier to maintain in the future.
 */
public interface Constants {
    final static String EMPTY_JSON = "{}";
}
